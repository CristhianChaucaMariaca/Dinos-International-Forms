extends ../templates/template-dishes
include ../mixins/platos
block append config
    - page = menu_kids.name
    - metadescripcion = menu_kids.descripcion
block content
    .container
        .dishes
            // TODO: * Modificar el | INCLUDE | y la variable de objetos
            each plate in menu_kids.content
                if(plate.status === true)
                    +platos(plate,menu_kids.category)