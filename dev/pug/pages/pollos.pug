extends ../templates/template-dishes
include ../mixins/platos
block append config
    - page = pollos.name
    - metadescripcion = pollos.descripcion
block content
    .container
        .dishes
            // TODO: * Modificar el | INCLUDE | y la variable de objetos
            each plate in pollos.content
                if(plate.status === true)
                    +platos(plate,pollos.category)